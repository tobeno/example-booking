import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#13d872",
      contrastText: "#444444",
    },
    secondary: {
      main: "#43b0ef",
      contrastText: "#444444",
    },
  },
});

export default theme;
