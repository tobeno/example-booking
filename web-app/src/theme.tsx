import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#EF8143",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#BC6866",
      contrastText: "#FFFFFF",
    },
  },
});

export default theme;
